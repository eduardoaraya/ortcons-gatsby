import React from 'react';
import Layout from '../../components/layout';

import { SectionAbout, HeaderBlack } from './styled';

const about = () => {
    return (
        <Layout transparentHeader={false}>
            <SectionAbout>

            </SectionAbout>
        </Layout>
    )
}

export default about;